import React, { Component } from 'react';
import { View } from 'react-native';

export default () => (
    <View
        style={{
            height: 1,
            backgroundColor: 'black',
            marginTop: 10,
            marginBottom: 10,
        }}
    />
);