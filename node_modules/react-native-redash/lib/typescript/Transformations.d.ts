import Animated from "react-native-reanimated";
import { TransformsStyle } from "react-native";
import { Point } from "./Coordinates";
declare type AnimatedTransform = {
    [P in keyof TransformsStyle["transform"]]: Animated.Adaptable<TransformsStyle["transform"][P]>;
};
export declare const translateZ: (perspective: Animated.Adaptable<number>, z: Animated.Adaptable<number>) => {
    scale: Animated.Node<number>;
};
export declare const translate: ({ x: translateX, y: translateY }: Point) => ({
    translateX: Animated.Adaptable<number>;
    translateY?: undefined;
} | {
    translateY: Animated.Adaptable<number>;
    translateX?: undefined;
})[];
export declare const transformOrigin: ({ x, y }: Point, ...transformations: AnimatedTransform[]) => AnimatedTransform[];
export {};
